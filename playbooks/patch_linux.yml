---
- name: Patch Red Hat Servers
  hosts: linux
  become: true
  become_method: sudo
  gather_facts: yes
  tasks:
    - name: Update yum packages
      yum:
        name: '*'
        state: latest
      when: ansible_os_family == "RedHat"
