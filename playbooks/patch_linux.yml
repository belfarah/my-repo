---
- name: Patch Red Hat Servers
  hosts: linux
  gather_facts: yes
  become: yes
  become_method: su
  become_user: root
  tasks:
    - name: Update yum packages
      yum:
        name: '*'
        state: latest
      when: ansible_os_family == "RedHat"
